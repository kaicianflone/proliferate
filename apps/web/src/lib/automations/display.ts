/**
 * Re-exports from the canonical run-status display module.
 * Kept for backwards compatibility with existing imports.
 */
export {
	normalizeProvider,
	getEventTypeLabel,
	getSeverityDotClass,
} from "@/lib/display/run-status";
